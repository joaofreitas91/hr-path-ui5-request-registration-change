_schema-version: "3.2"
ID: comaegearequestregistrationchange
description: Generated by Fiori Tools
version: 0.0.2
modules:
- name: comaegearequestregistrationchange-destination-content
  type: com.sap.application.content
  requires:
  - name: comaegearequestregistrationchange-destination-service
    parameters:
      content-target: true
  - name: comaegearequestregistrationchange-repo-host
    parameters:
      service-key:
        name: comaegearequestregistrationchange-repo-host-key
  - name: comaegearequestregistrationchange-uaa
    parameters:
      service-key:
        name: comaegearequestregistrationchange-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comaegearequestregistrationchange_html_repo_host
          ServiceInstanceName: comaegearequestregistrationchange-html5-service
          ServiceKeyName: comaegearequestregistrationchange-repo-host-key
          sap.cloud.service: comaegearequestregistrationchange
        - Authentication: OAuth2UserTokenExchange
          Name: comaegearequestregistrationchange_uaa
          ServiceInstanceName: comaegearequestregistrationchange-xsuaa-service
          ServiceKeyName: comaegearequestregistrationchange-uaa-key
          sap.cloud.service: comaegearequestregistrationchange
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comaegearequestregistrationchange-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comaegearequestregistrationchange-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comaegearequestregistrationchange.zip
      name: comaegearequestregistrationchange
      target-path: resources/
- name: comaegearequestregistrationchange
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comaegearequestregistrationchange-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comaegearequestregistrationchange-destination-service
    service-plan: lite
- name: comaegearequestregistrationchange-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: comaegearequestregistrationchange-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-name: comaegearequestregistrationchange-xsuaa-service
    service-plan: application
- name: comaegearequestregistrationchange-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comaegearequestregistrationchange-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
